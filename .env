# Used by docker-compose and the containers
NODE_ENV=production
API_PORT=3000
WEB_PORT=80
# If your API needs secrets, add them here (never commit real secrets!)
# DATABASE_URL=mysql://user:pass@db:3306/app
